#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){



    return 0;
}

/*

String Concatenate
strcat(str1, str2);

String Concatenate with specified number
strncat(str2, str1, 5);

String Compare
strcmp(str1, str2);

String Copy
strcpy(str1, str2);

String Length
strlen(str1, str2);

String Reverse
strrev(str);

String Lower
strlwr(str)

String Upper
strupr(str)

String Splitter
strtok(str, " ");
Example:
while(tok!=NULL) tok = strtok(NULL, " ");

*/